# PostgreSQL connection string
DATABASE_URL=

# Set to "false" only if your Postgres doesn't use SSL (e.g. local dev)
PGSSL=true

# The public URL this app is served at — used to build webhook URLs shown
# in the dashboard, e.g. https://mailstats.example.com
PUBLIC_BASE_URL=

SESSION_SECRET=mkkhridapanmethnata
